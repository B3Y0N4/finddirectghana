// Single source of truth for the canonical site domain — used by sitemap.ts,
// robots.ts, metadata, JSON-LD, and og-image generation. Change it here once
// if the domain ever moves again, instead of hunting down every literal.
export const SITE_URL = 'https://finddirectgh.com'
